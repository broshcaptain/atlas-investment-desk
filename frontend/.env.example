# Bu dosyayı .env.local olarak kopyalayın. FastAPI backend'in adresi.
# Belirtilmezse http://localhost:8000 varsayılır (backend/main.py'nin yerel varsayılan portu).
API_BASE_URL=http://localhost:8000
